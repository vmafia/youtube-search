# Dev / test dependencies (not deployed to production)
# Install with: pip install -r requirements-dev.txt
-r requirements.txt
gunicorn==22.0.0
pytest==8.2.2
pytest-cov==5.0.0
pytest-mock==3.14.0
google-genai
